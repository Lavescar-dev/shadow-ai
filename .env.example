# Local frontend-to-worker override. Leave unset in production when Worker is routed at /api/* on the same domain.
VITE_API_BASE_URL=http://127.0.0.1:8787

# Qwik static adapter origin. Use the final Pages/custom domain for production builds.
PUBLIC_ORIGIN=https://nexus.lavescar.com.tr
